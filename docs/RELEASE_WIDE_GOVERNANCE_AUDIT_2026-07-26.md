# تدقيق الحوكمة الشامل للإصدار 0.30.0-dev

## خط الأساس

```text
BASE-MAIN       = d72367c8e45a52368accc1d04ad542dc2c61756b
REVIEW-BRANCH   = review/release-wide-v0.30.0-dev
REVIEW-HEAD     = 8e20907d57056b978c5e91a28ab166a9ea9431a5
ISSUE           = #53 / OPEN
PR              = #54 / DRAFT / OPEN
LOCAL-BUILD     = PASS / 321 PAGES
QUALITY-CHECK   = PASS
RELEASE-READY   = NO
```

## الحكم المرحلي المحدّث

```text
VERDICT          = CHANGES-REQUIRED
BLOCKERS         = 0
MAJORS           = 2
MINORS           = 2
RELEASE-READY    = NO
OWNER-ADOPTION   = NOT REQUESTED
MERGE            = NOT AUTHORIZED
```

هذا حكم مرحلي لمسار مراجعة الإصدار، وليس حكمًا على الصحة العلمية لكل فصل على حدة.

## إغلاق BLOCKER-1 — محارف قائمة المراجع

كان سجل البناء الكامل يكرر رسائل من النمط:

```text
Missing character: There is no <Arabic glyph> in font Latin Modern Roman ...
```

أُغلق الحاجز على فرع المراجعة عبر:

1. استبدال غلاف المراجع العام من بيئة `english` إلى بيئة اتجاه `LTR` من دون فرض خط لاتيني على الحقول العربية.
2. تصحيح أربع صيغ عربية داخل رياضيات الفصل الثامن من `\mathrm{أولي}` إلى `\text{أولي}`.
3. إعادة البناء الكامل والتحقق من أن تحذيرات المحارف العربية المقروءة أصبحت صفرًا.
4. حصر التحذيرات المتبقية في 25 محرف تحكم غير مرئي من نوع `RLM` (`U+200F`) داخل Latin Modern Roman.

الحالة:

```text
ARABIC-READABLE-MISSING-GLYPHS = 0 / CLOSED
RLM-WARNINGS                    = 25 / NON-VISIBLE TECHNICAL NOISE
SOURCE-BOM                      = CLOSED
```

لا يوجد دليل حالي على فقد نص عربي مرئي في الناتج.

## إغلاق RW-MAJOR-01 — خارطة الطريق

حُدث `docs/ROADMAP.md` ليعكس:

- اكتمال الفصول 22--26 واعتمادها ودمجها.
- فتح مرحلة مراجعة الإصدار `0.30.0-dev`.
- إبقاء الإحصاء الحسابي والمراجعة السنوية امتدادين مستقبليين.

الحالة: `CLOSED`.

## إغلاق RW-MAJOR-02 — قائمة المهام

حُولت `docs/TODO.md` من قائمة متوقفة عند الفصل 21 إلى قائمة مراجعة الإصدار الشاملة.

الحالة: `CLOSED`.

## RW-MAJOR-03 — سجل النتائج المركزي غير متزامن

`docs/RESULTS_REGISTRY.md` لا يزال يحتاج مصالحة منهجية مع السجلات الفصلية للفصول 7--26.

### المطلوب

1. مطابقة السجلات الفصلية مع السجل المركزي.
2. إدراج جميع المعرفات المفعلة مع تصنيفاتها الدقيقة.
3. منع ترقية الخرائط والمبادئ المنهجية إلى `PROVED-HERE`.
4. تحديث تاريخ المراجعة وإضافة إيصال تحقق من فرادة المعرفات.

الحالة: `OPEN / MAJOR`.

## RW-MAJOR-04 — سجل التغييرات متوقف عند الفصل 21

قسم `[غير منشور]` في `CHANGELOG.md` لا يزال يحتاج تسجيل دورات الفصول 22--26 وإغلاق بنية المخطوط وفتح مراجعة الإصدار.

### المطلوب

إضافة ملخصات حوكمة دقيقة للفصول 22--26، البناء الكامل من 321 صفحة، وفتح Issue #53 وDraft PR #54، دون إعلان إصدار منشور أو `RELEASE-READY`.

الحالة: `OPEN / MAJOR`.

## إغلاق تجاوزات الأسطر الكبيرة

أُجري حصر موجّه لكل `Overfull \hbox` بقياس 20 نقطة أو أكثر، ثم عولجت الأسباب التالية:

- مسارات ملفات الحوكمة الطويلة في الفصول 9 و10 و12.
- SHA والأحكام الإنجليزية الطويلة في الفصلين 15 و16.
- سلسلتان مفاهيميتان طويلتان في الفصلين 1 و12.
- روابط DOI وعناوين المراجع الإنجليزية عبر `xurl` و`\raggedright` و`\emergencystretch`.
- ثلاثة مواضع أخيرة في عناوين الفصلين 12 و25 وفقرة الفصل 26.

التحقق النهائي المبلغ عنه محليًا:

```text
OVERFULL-HBOX >= 20pt = 0
```

الحالة: `CLOSED`.

## ديون تحريرية غير حاجزة

1. قد تبقى تجاوزات أصغر من 20 نقطة تحتاج فحصًا بصريًا عيناتيًا، لكنها لم تُصنف حاجزًا.
2. يبقى التقاط SHA-256 النهائي لآخر بناء بعد اكتمال جميع تعديلات الحوكمة مطلوبًا لإيصال الإغلاق النهائي.

## ما اجتاز

- الإصدار المركزي متسق عند `0.30.0-dev`.
- البناء الكامل نجح من 321 صفحة.
- فحص الجودة البرمجي نجح.
- ترتيب الفصول يصل إلى الفصل 26 وهو الأخير.
- PR #52 مدمج وIssue #51 مغلق.
- نتائج الفصل 26 مفعلة في سجله الفصلي بعد اعتماد المالك.
- محارف العربية المقروءة المفقودة = صفر.
- جميع تجاوزات `Overfull \hbox` بقياس 20 نقطة أو أكثر مغلقة.
- فرع المراجعة المبلغ عنه محليًا نظيف بعد الالتزام `8e20907`.

## ترتيب الإغلاق المتبقي

1. مصالحة `docs/RESULTS_REGISTRY.md` مع السجلات الفصلية والتحقق من فرادة المعرفات.
2. تحديث `CHANGELOG.md` حتى الفصل 26 ومرحلة مراجعة الإصدار.
3. فحص الروابط ومفاتيح BibTeX.
4. إجراء فحص بصري عيناتي للفهرس، بدايات الأجزاء، الفصول 22--26، والمراجع.
5. إعادة البناء النهائي، التقاط SHA-256، وإصدار تقرير مراجعة مستقل نهائي.
6. طلب اعتماد المالك للحكم النهائي فقط بعد إغلاق البنود السابقة.

حتى ذلك الحين:

```text
PR #54        = DRAFT
MERGE         = NOT AUTHORIZED
RELEASE-READY = NO
```