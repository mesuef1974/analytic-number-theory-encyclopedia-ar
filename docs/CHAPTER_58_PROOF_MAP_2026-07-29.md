# خريطة برهان الفصل الثامن والخمسين

```text
CHAPTER  = 58
STATUS   = PRE-AUTHORING
TITLE    = متباينة هيلبرت ومدخل إلى طريقة القيم الكبيرة
```

## ملاحظة على مسار الوصول إلى هذه الخريطة

سبقت هذه الخريطةَ **محاولتان فاشلتان** لإعادة بناء برهان مبرهنة القيم
الكبيرة الكاملة من شذرات بحث الويب؛ كُشِف في كل محاولة خطأ رياضي حقيقي
بالتحقق الذاتي قبل أي كتابة (موثَّق في `EVIDENCE_LEDGER`، قسم "تصحيح
ذاتي"). قُلِّص النطاق بقرار مالك صريح إلى نتيجة واحدة مُثبَتة بالكامل
(متباينة هيلبرت بثابت شور الحاد)، وهي النتيجة الوحيدة في هذا الفصل التي
تحقَّقتُ من برهانها **بالكامل وعدديًّا** قبل تسجيلها هنا. تفصيل التحقق
العددي في القسم الأخير.

## سلسلة النتائج المقترحة

| # | النتيجة | نوعها | البرهان | الاعتماد |
|---|---|---|---|---|
| ١ | متسلسلة فورييه للدالة السنّية: \(\sum_{k\ge1}\frac{\sin(2\pi kx)}{k}=\pi(\tfrac12-x)\) لِـ\(0<x<1\) | لمّة | كلاسيكية أولية (تُستشهَد، لا تُعاد برهنتها) | — |
| ٢ | \(g(x):=\sum_{k\ne0}\frac{e(kx)}{k}=i\pi(1-2x)\) لِـ\(0<x<1\)، ومنه \(\lvert g(x)\rvert\le\pi\) | لمّة | مباشر من (١) بجمع الحدّين \(\pm k\) | (١) |
| ٣ | **هوية التمثيل التكاملي**: \(\sum_{m\ne n}\frac{z_m\overline{z_n}}{n-m}=\int_0^1\lvert f(x)\rvert^2g(x)\,dx\) حيث \(f(x)=\sum_n z_ne(nx)\) | لمّة | تبديل الجمع والتكامل + تعامد \(\int_0^1e(jx)dx=[j=0]\) | (٢) |
| ٤ | **متباينة هيلبرت بثابت شور**: \(\bigl\lvert\sum_{m\ne n}\frac{z_m\overline{z_n}}{n-m}\bigr\rvert\le\pi\sum_n\lvert z_n\rvert^2\) | مبرهنة | من (٣): تقدير \(\lvert g\rvert\le\pi\) ثم مساواة بارسيفال \(\int_0^1\lvert f\rvert^2=\sum\lvert z_n\rvert^2\) | (٢)، (٣)، بارسيفال |
| ٥ | تلاشي الصيغة عند معاملات حقيقية: \(z_n\in\mathbb{R}\ \Rightarrow\) الصيغة \(=0\) | ملاحظة | النواة \(1/(n-m)\) متخالفة التناظر والأوزان \(z_mz_n\) متناظرة | (لا شيء) |
| ٦ | حدّة الثابت \(\pi\) (لا يمكن تحسينه) | نتيجة مُستشهَدة | **لا يُبرهَن هنا** — Schur 1911 (وHardy–Littlewood–Pólya، المبرهنة 294) | استشهاد |
| ٧ | تعميم مونتغمري–فون الموزون (تباعد أدنى \(\delta\) غير منتظم) | نتيجة مُستشهَدة | **لا يُبرهَن هنا** — Montgomery–Vaughan 1974 | استشهاد |
| ٨ | مبرهنة مونتغمري للقيم الكبيرة لكثيرات حدود ديريشليه | نتيجة مُستشهَدة | **لا يُبرهَن هنا** — Montgomery 1969 | استشهاد |

## الحكم على مستوى البرهان لكل نتيجة

- **PROVED-HERE:** النتائج ٢، ٣، ٤، ٥. سلسلة قصيرة مكتملة ومغلقة
  منطقيًّا: (١) مُستشهَدة \(\to\) (٢) \(\to\) (٣) \(\to\) (٤)، مع (٥)
  ملاحظة مستقلة أولية.
- **CITED:** النتائج ١ (متسلسلة فورييه الكلاسيكية للدالة السنّية)، ٦
  (حدّة \(\pi\))، ٧، ٨.
- **مساواة بارسيفال** مُدخل قياسي مُستشهَد به، لا يُعاد بناؤه (مطابقةً
  لكيفية تعامل المجلد الأول مع أدوات التحليل القياسية).

## تحذير نطاق إلزامي في متن الفصل

يجب أن ينص المتن صراحةً على أن:

1. النتيجة ٦ (حدّة الثابت) **مُستشهَدة لا مبرهَنة هنا** — يُقدَّم بدلًا
   منها شاهد عددي (القسم أدناه) لا يقوم مقام البرهان.
2. النتيجتان ٧ و٨ **مُستشهَدتان**، وهما — لا النتيجة ٤ — الأداتان
   المستعملتان فعليًّا في الفصل ٥٩ لاشتقاق حدّ الكثافة
   \(N(\sigma,T)\ll T^{4(1-\sigma)}\log^{O(1)}T\). النتيجة ٤ المُثبَتة هنا
   هي **الحالة الكلاسيكية المتباعدة بانتظام فقط**، وهي غير كافية بذاتها
   لتطبيق الكثافة.
3. لا يُدَّعى في أي موضع أن هذا الفصل يعيد بناء مبرهنة القيم الكبيرة.

هذا التحذير جوهري: بدونه يقرأ القارئ سلسلةً تبدأ بمتباينة هيلبرت وتنتهي
بحدّ كثافة، فيستنتج خطأً أن الطريق كامل داخليًّا — وهو ليس كذلك.

## التحقق العددي المُنجَز (قبل التأليف، لا بعده)

نُفِّذ على `numpy` بدقة مزدوجة. **النصان محفوظان في المستودع وقابلان
لإعادة التشغيل** (لا أرقام مذكورة من تشغيل عابر):

```bash
python scripts/verify_hilbert_inequality.py
python scripts/verify_hilbert_sharpness.py
```

| البند المُتحقَّق | الطريقة | النتيجة |
|---|---|---|
| النتيجة ١ (الدالة السنّية) | مجموع جزئي بـ\(2\times10^5\) حدًّا عند 7 قيم \(x\) | أقصى خطأ \(1.6\times10^{-5}\) (متوافق مع بطء تقارب المتسلسلة الشرطية) |
| النتيجة ٢ (\(g\) و\(\lvert g\rvert\le\pi\)) | مقارنة مباشرة بـ\(i\pi(1-2x)\) | أقصى خطأ \(3.2\times10^{-5}\)؛ الحد \(\pi\) محقَّق |
| **النتيجة ٣ (الهوية التكاملية)** | مقارنة الصيغة الثنائية بتكامل عددي (\(4\times10^5\) عقدة) عند \(N=2,4,5,9,12\) | أقصى فرق \(3.3\times10^{-9}\) (يتدرّج مع \(N\) كما يُتوقَّع من خطأ التربيع) — **الخطوة المحورية، مؤكَّدة** |
| النتيجة ٤ (المتباينة) | 400 تجربة عشوائية، \(N\in[2,40]\)، معاملات مركبة | أقصى نسبة \(\lvert\text{form}\rvert/(\pi\sum\lvert z\rvert^2)=0.4098\le1\) |
| النتيجة ٥ (التلاشي الحقيقي) | 5 تجارب بمعاملات حقيقية | \(\lvert\text{form}\rvert\lesssim2\times10^{-15}\) (صفر عدديًّا) |
| النتيجة ٦ (حدّة \(\pi\)، شاهد لا برهان) | أكبر قيمة ذاتية مطلقة لِـ\(iK\) (هرميتية) | \(N=10\!:\!0.7907\)، \(50\!:\!0.9471\)، \(200\!:\!0.9847\)، \(1000\!:\!0.9965\)، \(4000\!:\!0.9990\) — تتزايد رتيبًا نحو \(\pi\) من الأسفل ولا تتجاوزه |

**خطأ ذاتي مكتشف أثناء هذا التحقق ومُصحَّح:** أول محاولة لفحص حدّة الثابت
استعملت متتاليات **حقيقية** (\(z_n=n^{-1/2}\))، فأعطت نسبًا تقترب من الصفر
لا من الواحد. السبب أن الصيغة تتلاشى هويةً عند المعاملات الحقيقية
(النتيجة ٥ أعلاه) — فكان الفحص يقيس صفرًا. أُعيد الفحص بحساب المعيار
المؤثِّري (القيمة الذاتية العظمى لِـ\(iK\)) فظهر التقارب الصحيح إلى
\(\pi\). هذه الملاحظة أفادت الفصل مرتين: صحَّحت الفحص، وأنتجت النتيجة ٥
كملاحظة تعليمية قائمة بذاتها.

## تدقيق عدم الدور

- سلسلة (١)\(\to\)(٢)\(\to\)(٣)\(\to\)(٤) أحادية الاتجاه بلا حلقة.
- النتائج ٦، ٧، ٨ مُستشهَدة ولا يُستنتج منها شيء داخل هذا الفصل — تُذكر
  للسياق وللإحالة الأمامية إلى الفصل ٥٩ فقط.
- لا اعتماد على أي فصل لاحق. الفصل ١٨ (فان دير كوربوت) **غير مُستعمَل**
  في هذا المسار (ذُكر في `SCOPE` كاحتمال احتياطي لم يُحتَج إليه).
