# خريطة برهان الفصل الثامن والخمسين

```text
CHAPTER  = 58
DOC-TYPE = PRE-AUTHORING / PROOF-MAP  (نوع الوثيقة التاريخي، لا حالة
           الفصل الحالية: الفصل مؤلَّف فعلًا الآن)
CHAPTER-STATUS = AUTHORED-DRAFT / NON-CITABLE
TITLE    = متباينة هيلبرت ومدخل إلى طريقة القيم الكبيرة
VERSION  = 0.32.0-dev (وسم خط الفرع الحالي)
VERSION-AUTHORITY = docs/VERSION.md لا يزال 0.30.0-dev؛ لم يُرفع الإصدار
                     ولن يُرفع إلا بقرار مالك عند إغلاق مسار إصدار. وسم
                     0.32.0-dev هنا يطابق اسم الفرع فقط، ولا يُفهم كإعلان
                     إصدار جديد.
```

## ملاحظة على مسار الوصول إلى هذه الخريطة

سبقت هذه الخريطةَ **محاولتان فاشلتان** لإعادة بناء برهان مبرهنة القيم
الكبيرة الكاملة من شذرات بحث الويب؛ كُشِف في كل محاولة خطأ رياضي حقيقي
بالتحقق الذاتي قبل أي كتابة (موثَّق في `EVIDENCE_LEDGER`، قسم "تصحيح
ذاتي"). قُلِّص النطاق بقرار مالك صريح إلى نتيجة واحدة مُثبَتة بالكامل
(متباينة هيلبرت بالثابت \(\pi\)؛ المتراجحة بالثابت \(\pi\) مبرهَنة داخليًّا؛ حدّة \(\pi\) مُستشهَد بها)، وهي النتيجة الوحيدة في هذا الفصل التي
تحقَّقتُ من برهانها **بالكامل وعدديًّا** قبل تسجيلها هنا. تفصيل التحقق
العددي في القسم الأخير.

## سلسلة النتائج المقترحة

**اصطلاح ثابت في كل ما يلي:** \(N\in\mathbb{N}\) و\(z_1,\dots,z_N\in\mathbb{C}\)
متتالية **منتهية**، و\(e(y):=e^{2\pi iy}\)، و\(f(x):=\sum_{n=1}^{N}z_ne(nx)\)
(مجموع منته، فلا تنشأ مسائل تقارب في \(f\)). المقام المعتمد في المتن هو
\(n-m\)؛ صيغة \(m-n\) هي سالبها وتتكافأ معها تحت القيمة المطلقة (يجب
توحيد الاصطلاح في المتن ولو مرة واحدة صراحةً).

| # | النتيجة | نوعها | البرهان | الاعتماد |
|---|---|---|---|---|
| ١ | متسلسلة فورييه للدالة السنّية: \(\sum_{k\ge1}\frac{\sin(2\pi kx)}{k}=\pi(\tfrac12-x)\) لِـ\(0<x<1\) | لمّة | كلاسيكية أولية (تُستشهَد، لا تُعاد برهنتها) | — |
| ٢ | \(g(x):=i\pi(1-2x)\) على \((0,1)\) هي مجموع \(\sum_{k\ne0}\frac{e(kx)}{k}\)، ومنه \(\lvert g(x)\rvert<\pi\) لكل \(x\in(0,1)\) | لمّة | مباشر من (١) بجمع الحدّين \(\pm k\): \(\sum_{k\ne0}\frac{e(kx)}{k}=2i\sum_{k\ge1}\frac{\sin(2\pi kx)}{k}\) | (١) |
| ٢أ | \(\int_0^1e(jx)\,dx=0\) و\(\int_0^1x\,e(jx)\,dx=\frac{1}{2\pi ij}\) لكل \(j\in\mathbb{Z}\setminus\{0\}\) | لمّة | تكامل بالتجزئة أولي | — |
| ٣ | **معامِلات فورييه لِـ\(g\) بصيغتها المغلقة**: \(\int_0^1e(jx)g(x)\,dx=-\frac1j\) لِـ\(j\ne0\)، و\(=0\) لِـ\(j=0\) | لمّة | تكامل **الصيغة المغلقة** \(i\pi(1-2x)\) من (٢) ضد \(e(jx)\)، باستعمال (٢أ) — **لا تبديل بين تكامل ومتسلسلة** | (٢)، (٢أ) |
| ٤ | **هوية التمثيل التكاملي**: \(\int_0^1\lvert f(x)\rvert^2g(x)\,dx=\sum_{m\ne n}\frac{z_m\overline{z_n}}{n-m}\) | لمّة | فكّ \(\lvert f\rvert^2=\sum_{m,n}z_m\overline{z_n}e((m-n)x)\) (مجموع **منته**، فتبديله مع التكامل تافه)، ثم تطبيق (٣) على \(j=m-n\): الحدود القطرية تُلغى (\(j=0\))، وغير القطرية تعطي \(-1/(m-n)=1/(n-m)\) | (٣) |
| ٥ | **متباينة هيلبرت بالثابت \(\pi\)** (المتراجحة فقط؛ الحدّة نتيجة ٨ مُستشهَدة): \(\bigl\lvert\sum_{m\ne n}\frac{z_m\overline{z_n}}{n-m}\bigr\rvert\le\pi\sum_{n=1}^{N}\lvert z_n\rvert^2\) | مبرهنة | من (٤): \(\bigl\lvert\int_0^1\lvert f\rvert^2g\bigr\rvert\le\sup_{(0,1)}\lvert g\rvert\cdot\int_0^1\lvert f\rvert^2\le\pi\int_0^1\lvert f\rvert^2\)، والتعامد يعطي \(\int_0^1\lvert f\rvert^2=\sum_n\lvert z_n\rvert^2\) | (٢)، (٤) |
| ٦ | تلاشي الصيغة عند معاملات حقيقية: \(z_n\in\mathbb{R}\ \Rightarrow\) الصيغة \(=0\) | ملاحظة | النواة \(1/(n-m)\) متخالفة التناظر والأوزان \(z_mz_n\) متناظرة | (لا شيء) |
| ٧ | المتباينة في (٥) **صارمة** لكل \(z\ne0\)؛ ومعيار المقطع المنته \(<\pi\) لكل \(N\) | ملاحظة | \(\lvert g\rvert<\pi\) صارمًا داخل \((0,1)\) (لا يقارب \(\pi\) إلا عند الطرفين، حيث المجموعة مقياسها صفر) | (٢) |
| ٨ | حدّة الثابت \(\pi\) (لا يمكن تحسينه) | نتيجة مُستشهَدة | **لا يُبرهَن هنا** — **Schur 1911 وحده** (لا HLP/294: تلك تبرهن المتراجحة لا الأمثلية — قراءة مباشرة 2026-07-30) | استشهاد |
| ٩ | تعميم مونتغمري–فون الموزون (تباعد أدنى غير منتظم \(\delta_n\)) | نتيجة مُستشهَدة | **لا يُبرهَن هنا** — Montgomery–Vaughan 1974 | استشهاد |
| ١٠ | مبرهنة مونتغمري للقيم الكبيرة لكثيرات حدود ديريشليه | نتيجة مُستشهَدة | **لا يُبرهَن هنا** — Montgomery 1969 | استشهاد |
| ١١ | **جسر التكافؤ** بين الصيغة الثنائية الحقيقية (صيغة HLP) والصيغة التربيعية المركّبة (صيغة الفصل): \(S(z)=-2i\,x^{\mathsf T}Ky\) لِـ\(z=x+iy\) — `ANT-PRIN-58-02` | مبدأ منهجي | مباشر من تخالف تناظر \(K\)؛ ينقل المتراجحة بمتوسطية الحسابي-الهندسي، وينقل **الحدّة** باختيار \(\lVert x\rVert=\lVert y\rVert=1/\sqrt2\) | (٦) |
| ١٢ | **تطابق المعيارين** \(B_N=Q_N\) لكل \(N\ge2\) منتهٍ — `ANT-PROP-58-07` | قضية | \(K^{\mathsf T}K=-K^2=(iK)^2\)، فالقيم المفردة لـ\(K\) هي \(\lvert\mu_j\rvert\) للقيم الذاتية \(\mu_j\) لـ\(iK\)؛ و\(Q_N\) نصف القطر الطيفي لهرميتية. **مبرهَنة عمومًا، لا شاهدًا عدديًّا** | (١١) |

## الحكم على مستوى البرهان لكل نتيجة

- **PROVED-HERE (٨):** النتائج ٢، ٢أ، ٣، ٤، ٥، ٦، ٧، ١٢. السلسلة
  الأساسية مغلقة منطقيًّا: (١) مُستشهَدة \(\to\) (٢) \(+\) (٢أ)
  \(\to\) (٣) \(\to\) (٤) \(\to\) (٥)، مع (٦) و(٧) ملاحظتين أوليتين،
  و(١٢) تطابق المعيارين مبرهَنًا عمومًا.
- **METHODOLOGICAL-PRINCIPLE (٢):** `ANT-PRIN-58-01` (تحذير النطاق)
  و(١١) `ANT-PRIN-58-02` (جسر التكافؤ). وهما **ليستا** ضمن
  `PROVED-HERE`: الأولى حرس استدلالي، والثانية جسر تصنيفي بين صيغتين.
  (كانت (١١) مُدرَجة خطأً ضمن `PROVED-HERE` في مسودة سابقة.)
- أُضيفت (١١) و(١٢) بعد أن كشفت قراءة HLP المباشرة أن صيغة المصدر ثنائية
  حقيقية لا تربيعية مركّبة، فلزم جسر صريح ثم برهان عام للتطابق بدل
  الاكتفاء بشاهد عددي.
- **العدد الفعلي بعد التأليف:** **14** معرِّفًا =
  **8** `PROVED-HERE` + **2** `METHODOLOGICAL-PRINCIPLE`
  (`ANT-PRIN-58-01` تحذير النطاق، و`ANT-PRIN-58-02` جسر التكافؤ)
  + **4** `CITED`. والمبدآن المنهجيان ليسا نتيجتين رياضيتين مستقلتين،
  فلا يُعَدّان ضمن `PROVED-HERE`. مسجَّلة كلها في
  `RESULTS_REGISTRY_CHAPTER_58_DRAFT.md` بحالة `DRAFT / NON-CITABLE`.
  (تقدير ما قبل التأليف كان سبعًا وأربعًا؛ الفارق من نتائج أُضيفت أثناء
  معالجة مراجعات ما بعد التأليف.)
- **CITED:** النتيجة ١ (متسلسلة فورييه الكلاسيكية للدالة السنّية)،
  والنتائج ٨ (حدّة \(\pi\))، ٩، ١٠.
- **لا حاجة إلى مساواة بارسيفال كمُدخل خارجي:** المطلوب في (٥) هو فقط
  \(\int_0^1\lvert f\rvert^2=\sum_n\lvert z_n\rvert^2\) لمجموع **منته**،
  وهذا هو تعامد \(\{e(nx)\}\) نفسه المستعمل في (٣)/(٤) — لا مبرهنة
  إضافية. (نبَّهت المراجعة المستقلة إلى أن استدعاء بارسيفال هنا كان
  تحميلًا زائدًا.)

## تحذير نطاق إلزامي في متن الفصل

يجب أن ينص المتن صراحةً على أن:

1. النتيجة ٨ (حدّة الثابت) **مُستشهَدة لا مبرهَنة هنا** — يُقدَّم بدلًا
   منها شاهد عددي (القسم أدناه) لا يقوم مقام البرهان.
2. النتيجتان ٩ و١٠ **مُستشهَدتان**، وهما — لا النتيجة ٥ — الأداتان
   المستعملتان فعليًّا في الفصل ٥٩ لاشتقاق حدّ الكثافة
   \(N(\sigma,T)\ll T^{4(1-\sigma)}\log^{O(1)}T\). النتيجة ٥ المُثبَتة هنا
   هي **الحالة الكلاسيكية المتباعدة بانتظام فقط** (\(\lambda_n=n\))، وهي
   غير كافية بذاتها لتطبيق الكثافة، الذي يحتاج \(\lambda_n=\log n\) غير
   منتظمة التباعد ومن ثَمّ الصيغة الموزونة (٩)، إضافةً إلى التقطيع (١٠).
3. لا يُدَّعى في أي موضع أن هذا الفصل يعيد بناء مبرهنة القيم الكبيرة.

هذا التحذير جوهري: بدونه يقرأ القارئ سلسلةً تبدأ بمتباينة هيلبرت وتنتهي
بحدّ كثافة، فيستنتج خطأً أن الطريق كامل داخليًّا — وهو ليس كذلك. أكَّدت
المراجعة المستقلة صحة هذا التحذير رياضيًّا، وطلبت تكراره في `SCOPE`
أيضًا (نُفِّذ).

## التحقق العددي المُنجَز (قبل التأليف، لا بعده)

نُفِّذ على `numpy` بدقة مزدوجة. **النصان محفوظان في المستودع وقابلان
لإعادة التشغيل** (لا أرقام مذكورة من تشغيل عابر):

```bash
python scripts/verify_hilbert_inequality.py
python scripts/verify_hilbert_sharpness.py
```

| البند المُتحقَّق | الطريقة | النتيجة |
|---|---|---|
| النتيجة ١ (الدالة السنّية) | مجموع جزئي بـ\(2\times10^5\) حدًّا عند 7 قيم \(x\) | أقصى خطأ \(1.6\times10^{-5}\) (متوافق مع بطء تقارب المتسلسلة الشرطية) |
| النتيجة ٢ (\(g\) و\(\lvert g\rvert<\pi\)) | مقارنة مباشرة بـ\(i\pi(1-2x)\) | أقصى خطأ \(3.2\times10^{-5}\)؛ الحد \(\pi\) محقَّق |
| النتيجة ٣ (معامِلات فورييه لِـ\(g\)) | تكامل عددي (\(2\times10^6\) عقدة) لِـ\(\int_0^1e(jx)g(x)dx\) عند \(j=-5,-3,-1,1,2,3,4,7\) ومقارنته بـ\(-1/j\)؛ وحالة \(j=0\) | أقصى فرق \(2.9\times10^{-12}\)؛ حالة \(j=0\) تعطي صفرًا بالضبط — **الخطوة التي تُغني عن تبديل المتسلسلة والتكامل** |
| **النتيجة ٤ (الهوية التكاملية)** | مقارنة الصيغة الثنائية بتكامل عددي (\(4\times10^5\) عقدة) عند \(N=2,4,5,9,12\) | أقصى فرق \(3.3\times10^{-9}\) (يتدرّج مع \(N\) كما يُتوقَّع من خطأ التربيع) — **الخطوة المحورية، مؤكَّدة** |
| النتيجة ٥ (المتباينة) | 400 تجربة عشوائية، \(N\in[2,39]\)، معاملات مركبة | أقصى نسبة \(\lvert\text{form}\rvert/(\pi\sum\lvert z\rvert^2)=0.4098\le1\) |
| النتيجة ٦ (التلاشي الحقيقي) | 5 تجارب بمعاملات حقيقية | \(\lvert\text{form}\rvert\lesssim2\times10^{-15}\) (صفر عدديًّا) |
| النتيجتان ٧ و٨ (الصرامة، وحدّة \(\pi\) — شاهد لا برهان) | أكبر قيمة ذاتية مطلقة لِـ\(iK\) (هرميتية)، **مقسومةً على \(\pi\)** كما يطبعها النصّ | النسبة \(\lVert\cdot\rVert/\pi\): \(N=10\!:\!0.7907\)، \(50\!:\!0.9471\)، \(200\!:\!0.9847\)، \(1000\!:\!0.9965\)، \(4000\!:\!0.9990\) — تتزايد رتيبًا نحو \(1\) من الأسفل، وتبقى \(<1\) لكل \(N\) منته، أي أن المعيار نفسه \(<\pi\) دائمًا (مطابق للنتيجة ٧) |

**خطأ ذاتي مكتشف أثناء هذا التحقق ومُصحَّح:** أول محاولة لفحص حدّة الثابت
استعملت متتاليات **حقيقية** (\(z_n=n^{-1/2}\))، فأعطت نسبًا تقترب من الصفر
لا من الواحد. السبب أن الصيغة تتلاشى هويةً عند المعاملات الحقيقية
(النتيجة ٦ أعلاه) — فكان الفحص يقيس صفرًا. أُعيد الفحص بحساب المعيار
المؤثِّري (القيمة الذاتية العظمى لِـ\(iK\)) فظهر التقارب الصحيح إلى
\(\pi\). هذه الملاحظة أفادت الفصل مرتين: صحَّحت الفحص، وأنتجت النتيجة ٦
كملاحظة تعليمية قائمة بذاتها.

**صحة حجّة المعيار المؤثِّري (أكَّدتها المراجعة المستقلة):** الصيغة تساوي
\(i\cdot z^{*}(iK)z\) حيث \(K_{mn}=1/(n-m)\)، و\(iK\) هرميتية، فسوپريمم
\(\lvert\text{form}\rvert/\sum\lvert z_n\rvert^2\) يساوي **بالضبط** أكبر
قيمة ذاتية مطلقة لِـ\(iK\). أي أن الفحص العددي يقيس فعلًا الكمية المُدَّعاة.

## تدقيق عدم الدور

- سلسلة (١)\(\to\)(٢)+(٢أ)\(\to\)(٣)\(\to\)(٤)\(\to\)(٥) أحادية الاتجاه
  بلا حلقة، و(٦) و(٧) مستقلتان.
- النتائج ٨، ٩، ١٠ مُستشهَدة ولا يُستنتج منها شيء داخل هذا الفصل — تُذكر
  للسياق وللإحالة الأمامية إلى الفصل ٥٩ فقط.
- لا اعتماد على أي فصل لاحق. الفصل ١٨ (فان دير كوربوت) **غير مُستعمَل**
  في هذا المسار: حُذف من ترويسة متطلبات `SCOPE` بعد المراجعة الأولى، ثم
  صُحِّحت فقرة في متن `SCOPE` كانت لا تزال تقول إنه «يُستعمَل» بعد أن
  نبَّهت المراجعة الضيقة الثانية إلى هذا التناقض المتبقّي.
- الفصل ٥٧ مطلوب فقط للإحالة الأمامية عند ذكر \(N(\sigma,T)\) في تحذير
  النطاق، لا كمدخل برهاني.
