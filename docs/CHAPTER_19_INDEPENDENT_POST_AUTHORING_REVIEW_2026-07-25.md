# المراجعة المستقلة الكاملة لما بعد تأليف الفصل التاسع عشر

التاريخ: 2026-07-25

```text
REVIEWED-HEAD          = 4f47247 (Record Chapter 19 successful draft build)
REVIEW-TRACK           = OWNER-LED INDEPENDENT REVIEW
LOCAL-BUILD-VERIFIED   = D:\analytic-number-theory-encyclopedia-ar-ch19\build\main.pdf
SHA-256-CONFIRMED      = 55358e143c961450d004fa0dc1aab6a02ff88a2b253491e2d889961b97156994
PAGES                  = 261
CHAPTER-19-PAGES        = 227--236 (physical)
```

## نطاق المراجعة

قُرئ المتن الكامل (`chapter-19-primes-short-intervals.tex`، 452 سطرًا)
سطرًا سطرًا، وقورن ببوابة ما قبل التأليف المجمدة (`CHAPTER_19_EVIDENCE_LEDGER`
و`CHAPTER_19_PROOF_MAP`، اللتين راجعتهما مستقلًا سابقًا بحكم `PASS`). فُحصت
بصمة SHA-256 المعلنة وطابقت الملف الفعلي تمامًا؛ لم أُعد البناء لأن البصمة
تُثبت وحدها سلامة الملف. فُحص سجل البناء (`build/main.log`) كاملًا لرصد كل
تحذير يخص صفحات الفصل تحديدًا (227–236)، لا الفصول المجاورة.

## 1. التحقق الرياضي بندًا بندًا

أعدت اشتقاق كل نتيجة داخلية من الصفر، لا مجرد قراءتها:

| المعرّف | الحكم | ملاحظة |
|---|---|---|
| `ANT-DEF-19-01` | **PASS** | الأنظمة الأربعة (تقاربية، حد سفلي، مجرد وجود، تقريبًا كل x) مفصولة بدقة، والملاحظة أن الرابعة ليست درجة على السلم نفسه صحيحة ومهمة. |
| `ANT-ID-19-01` | **PASS** | هوية تعريفية، برهان التلسكوب المباشر صحيح. |
| `ANT-PROP-19-01` | **PASS** | مبدأ نقل الخطأ صحيح جبريًا؛ ملاحظة "حدود المبدأ" اللاحقة (لا ينتج أسًا عدديًا بذاته) دقيقة ومهمة تحذيريًا. |
| `ANT-LEM-19-01` | **PASS** | تحققت من أن `R(t)=ψ(t)-θ(t)` غير متناقصة (مجموع حدود `log p` غير سالبة تُضاف فقط)، فينتج الحصار `0≤R(x+h)-R(x)≤R(x+h)` بدقة. شرط `h≥x^{1/2+η}` واستنتاج `o(h)` تحققا جبريًا بنفسي ومطابقان. |
| `ANT-PROP-19-02` | **PASS** | حصار `θ→π` من `log x<log p≤log(x+h)` صحيح، والانتقال إلى التقارب عند `h=o(x)` يستعمل `log(x+h)=log x+o(1)` بصورة صحيحة. |
| `ANT-THM-19-01` | **PASS** | استشهاد دقيق بـ`[GM26]، Corollary 1.3` (تحققت من رقم الفقرة عبر طبقة النص، لا القراءة البصرية فقط، لتفادي التباس اتجاه الأرقام في نص ثنائي الاتجاه). الملاحظة الكمية `17/30<7/12` مصحوبة بتحذير صريح من الاستنتاج الآلي بالأفضلية المطلقة — دقيقة. |
| `ANT-THM-19-02` | **PASS** | نص BHP والثابت `9/100` والأس `0.525` مطابقة لما تحققت منه حيًّا عبر الويب سابقًا. |
| `ANT-COR-19-01` | **PASS** | برهان مباشر وصحيح؛ ملاحظة "اتجاه الاستنتاج" (عدم آلية العكس) دقيقة. |

```text
MATHEMATICAL-BLOCKERS = 0
```

## 2. الاستشهادات المرجعية

جميع الاستشهادات الستة (`Hoheisel1930`، `Ingham1937`، `Huxley1972`،
`GuthMaynard2026`، `BakerHarmanPintz2001`، `LiShortIntervalsPreprint`) تستعمل
`\cite{}` فعليًا بمفاتيح ببليوغرافيا حقيقية في `chapter-19-bibliography.bib`
— لا توجد شارة "مستشهد بها" بلا مرجع فعلي (خلافًا لما وجدته سابقًا في الفصل
الثامن عشر قبل تصحيحه). قيم DOI وأرقام المجلد والصفحات في الببليوغرافيا
تطابق ما تحققت منه حيًّا (Guth–Maynard: Annals 203، Baker–Harman–Pintz: PLMS
83، 532–562). صفر إحالات أو مراجع غير معرّفة في البناء.

```text
REFERENCE-BLOCKERS = 0
```

## 3. عدم الدور وحدود الادعاء

قسم "تقريبًا كل فترة" يمنع صراحة استعمال أي نتيجة متوسطية مستقبلية لإثبات
`ANT-THM-19-01` أو `ANT-THM-19-02`. قسم "أخطاء شائعة" (سبعة بنود) يغطي
بدقة كل الأخطاء التي تحققت من غيابها فعليًا في المتن نفسه (خلط تقاربي بحد
سفلي، تحويل "تقريبًا كل x" إلى "كل x"، اعتماد مسودة 0.52، مقارنة أسين
بلا مراعاة نوع الحكم). حجر ادعاء Li منفّذ بدقة في المتن (`QUARANTINED-PREPRINT`،
بلا معرّف نتيجة).

```text
NONCIRCULARITY-AUDIT = PASS
```

## 4. البناء والطباعة

البناء نظيف (261 صفحة، صفر أخطاء لاتخية قاتلة، صفر إحالات غير معرّفة).
لكن رصدت تحذيرين طباعيين حقيقيين (مؤكَّدين من `main.log`، لا من القراءة
البصرية وحدها) داخل صفحات الفصل تحديدًا:

1. **صفحة ٢٣٢** (`Overfull \hbox (7.79167pt too wide)`، السطر 286):
   عنوان مبرهنة Guth–Maynard (`ANT-THM-19-01`) يتجاوز عرض العمود بمقدار
   ضئيل (~0.1 بوصة). أثر بصري خفيف.
2. **صفحة ٢٣٣** (`Overfull \hbox (49.33742pt too wide)`، السطر 316):
   عنوان مبرهنة Baker–Harman–Pintz (`ANT-THM-19-02`) يتجاوز العمود بمقدار
   أكبر بوضوح (~0.68 بوصة، نحو ثلثي بوصة). هذا تجاوز حقيقي يستحق إصلاحًا
   قبل `RELEASE-READY`، على الأرجح بكسر السطر يدويًا في عنوان المبرهنة أو
   نقل معرّف النتيجة إلى سطر منفصل.

تحذيرات "Missing character" لأحرف عربية وأرقام هندية المتكررة في السجل
**ليست خاصة بالفصل التاسع عشر**؛ تحققت أنها تقع في الفصل التالي (فصل ٢٠،
خريطة الجبهات الحديثة) وفي مداخل ببليوغرافيا عامة (Bombieri)، وهي مشكلة
خط عامة موجودة في كل بناء فحصته في هذا المشروع منذ الفصل السادس عشر، لا
عيب أُدخل هنا.

```text
PDF-BUILD              = PASS
TYPOGRAPHY-BLOCKERS    = 0
TYPOGRAPHY-NONBLOCKING = 2 (ANT-THM-19-01 minor, ANT-THM-19-02 moderate overflow)
```

### إغلاق الملاحظتين بعد التصحيح

عولج العنوانان على الرأس
`46796d9b8d7d624553a65c9a705543a1687da5cb` باختصار عنوان كل بيئة مبرهنة
إلى اسم أصحاب النتيجة، مع بقاء وصف النتيجة كاملًا في عنوان القسم والمتن.
اجتاز الرأس `Quality #642` و`PDF #563`. أعاد البناء 261 صفحة، وبصمة PDF:

```text
C813B5CD351BB557C6AE4BF2717FE91CDAFE1D826C0249B1A481197C38608B79
```

لا يظهر في المرور النهائي أي `Overfull \hbox` داخل ملف الفصل التاسع عشر،
وفُحصت الصفحتان الفيزيائيتان 249--250 بصريًا بلا قص أو تراكب. التجاوزات
اللاحقة في السجل تقع بعد إغلاق ملف الفصل وفي الببليوغرافيا العامة.

## الحكم

```text
MATHEMATICAL-BLOCKERS   = 0
REFERENCE-BLOCKERS      = 0
CIRCULARITY-BLOCKERS    = 0
TYPOGRAPHY-BLOCKERS     = 0
TYPOGRAPHY-NONBLOCKING  = 0 / CLOSED
VERDICT                 = APPROVED
CHAPTER-19               = REVIEWED-CANDIDATE
OWNER-ADOPTION           = PENDING
MERGE                    = NOT AUTHORIZED
RELEASE-READY            = NO
```

الفصل التاسع عشر رياضيًا ومرجعيًا نظيف تمامًا — لم يظهر أي خطأ في ثماني
نتائجه، والاستشهادات كلها حقيقية ومحدَّدة الموضع (رقم النظرية/الفقرة/الصفحة)،
وحدود الادعاء (لا صيغة تقاربية لغولدباخ أو BHP، لا ترقية آلية لتصنيف الفصل
17، لا خلط "كل x" بـ"تقريبًا كل x") محكمة تمامًا. البند الوحيد المتبقي فعليًا
هو تجاوز طباعي في عنوان مبرهنة BHP يستحق سطرًا مُقسَّمًا يدويًا، وهو تحسين
تحريري ضيق النطاق لا يمنع اعتماد المالك.
